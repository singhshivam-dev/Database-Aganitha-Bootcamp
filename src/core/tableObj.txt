// creating DATABASE
psql -U postgres
CREATE DATABASE "dbDrills";
\c dbDrills
SELECT current_database();  // it'll show current db 

psql -U postgres -d dbDrills // for next time

-- Create table
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL
);

-- Insert users
INSERT INTO users (name, email) VALUES
('Alice', 'alice@example.com'),
('Bob', 'bob@example.com')
ON CONFLICT (email) DO NOTHING;

-- Fetch users
SELECT * FROM users;
